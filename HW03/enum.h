#ifndef ENUM_H
#define ENUM_H
#include <iostream>
#include <fstream>
#include <cstdlib>

using namespace std;

//Defining enum for shapes.(Using for main)
enum class Shapes{
	Rectangle,
	Circle,
	Triangle
};

#endif